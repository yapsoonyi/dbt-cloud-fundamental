version: 2

sources:
    - name: jaffle_shop # using this in ref function
      database: raw
      schema: jaffle_shop
      tables:
          - name: customers
          - name: orders
            loaded_at_field: _etl_loaded_at # choose a timestamp column in the raw table
            freshness:
              warn_after: {count: 12, period: hour}
              error_after: {count: 24, period: hour}
